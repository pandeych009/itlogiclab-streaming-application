server: 
  port: ${PROD_PORT:20002} 
spring:
  application:
    name: config-client
  profiles: 
    active: stream-consumer-dev-app
  config:
    import: optional:configserver:http://localhost:8888
    name: stream-consumer-dev-app

logging:
  level:
    ROOT: INFO
    org.spring.framework: DEBUG